{
  "name": "solsignal-controller",
  "version": "0.1.0",
  "description": "SolSignal desktop controller (Tauri) — manages the Docker trading stack",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
